# AgentHub 重构进度清单与后续改进方案

> 评估依据：对照 [重构.md](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/重构.md) 的目标架构，遍历 `services/`、`platform/`、`deploy/`、`app/`、`frontend/` 全部代码与契约文件后得出。
> 评估时间：2026-06-29
> 结论：**当前处于 Phase 1 中后期**。Go 层已搭出 MVP 骨架并能跑通事件链路，Rust 层与 Python 离线层基本是空桩，遗留 Python 单体仍是唯一承载真实业务的运行体，可观测、K8s、双轨迁移均未启动。

---

## 一、总体进度矩阵

| 阶段 | 目标 | 完成度 | 状态 |
|------|------|--------|------|
| Phase 1（4–6 周）基础设施 + 契约 + Go Gateway MVP | 6 项交付物 | ~45% | 进行中 |
| Phase 2（6–8 周）Go 编排上线 + 会话态外置 + Python 退出在线主链路 | 4 项交付物 | ~15% | 未启动 |
| Phase 3（6–10 周）Rust 核心 + DeepSearch + 背压 | 4 项交付物 | ~5% | 仅占位 |
| Phase 4（8–12 周）多租户/配额/审计/灰度 + 万人压测 | 4 项交付物 | 0% | 未启动 |

### Phase 1 交付物逐项核对

| 交付物 | 状态 | 证据 |
|--------|------|------|
| 服务边界定稿 | ✅ 完成 | [platform/service_boundaries.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/service_boundaries.json) 列出 16 个服务职责与依赖 |
| 事件 schema 发布 | ✅ 完成 | [platform/contracts/event-catalog.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/contracts/event-catalog.json) + [event-envelope.schema.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/contracts/event-envelope.schema.json) + Go/Rust/Python 三端 envelope 结构体已对齐 |
| Redis/NATS/PG/Qdrant/MinIO 基线 | 🟡 部分 | [deploy/docker-compose.platform.yml](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/deploy/docker-compose.platform.yml) 起了 6 个基础设施，但**用单机 Redis/单节点 NATS/单节点 Qdrant**，未达 cluster 拓扑；NATS 用核心 NATS 而非 JetStream |
| Go Gateway MVP | 🟡 部分 | [gateway-service](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/gateway-service/cmd/gateway-service/main.go) 仅是 HTTP→NATS 事件发布器，**无 WebSocket、无 SSE、无 JWT、无限流** |
| 可观测 bootstrap | ❌ 未完成 | 全仓无 OpenTelemetry / Prometheus / Grafana / Loki / Tempo 任何配置 |

---

## 二、分层完成度详表

### 1. 接入层（Go）— 完成度 ~25%

| 服务 | 现状 | 缺口 |
|------|------|------|
| gateway-service | HTTP `/publish`、`/permissions/request` 把消息发到 NATS；`/healthz`、`/profile` | **无 WebSocket 网关、无 SSE fallback、无 JWT 鉴权、无租户限流、无连接注册表、无心跳/重连**。前端 [next.config.js](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/frontend/next.config.js) 仍把 `/api/*` 代理到 `127.0.0.1:8000`（遗留单体），完全未接入 Go 网关 |
| session-service | Redis presence / cursor / resume 代理 | **无 PostgreSQL 持久化、无消息 checkpoint**（cursor 只存连接级字符串，不存消息序列号）；`/resume` 调 stream-delivery 的内存 replay，重启即丢 |
| stream-delivery-service | 内存事件缓冲 + SSE 轮询 + replay | **无 WebSocket fanout、无 Rust stream-core 集成、无慢客户端背压、replay 仅内存**（[main.go:43](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/stream-delivery-service/cmd/stream-delivery-service/main.go#L43) 用 `SessionEventIndex` 内存 map） |

### 2. 实时编排层（Go）— 完成度 ~20%

| 服务 | 现状 | 缺口 |
|------|------|------|
| realtime-orchestrator | 订阅 NATS，关键词命中 `rm -rf/docker/kubectl` 等触发权限请求；发 stream chunk/flush/complete 生命周期事件 | **无真正 ReAct 状态机**（`stages` 只是列表常量，[main.go:36](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/realtime-orchestrator/cmd/realtime-orchestrator/main.go#L36)）；**无 Router/Planner/Executor/Critic/Summarizer/Search 多 Agent 分工**；**无 DeepSearch 编排**；**无 LLM 实际调用**；**无 PostgreSQL 持久化**；风险检测是字符串匹配而非策略引擎 |
| tool-permission-service | Redis 存请求/决策，发 resolved + audit 事件 | 功能相对完整，但**审计仅发事件未落 PostgreSQL/ClickHouse**；无 ABAC 策略评估、无二次确认 UI 协议 |
| agent-runtime-control-plane | 订阅 dispatch，回写 "completed" 结果 | **纯回声**（[main.go:41](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/agent-runtime-control-plane/cmd/agent-runtime-control-plane/main.go#L41) 直接构造 "runtime executed request"）；**无 worker lease、无预算控制、无并发上限、无 Python worker 派发、无 circuit breaker** |

### 3. 高性能核心层（Rust）— 完成度 ~3%

| Crate | 现状 | 缺口 |
|-------|------|------|
| platform-events | 定义 `EventEnvelope` 结构体 | ✅ 唯一有内容的 crate |
| stream-core | 10 行，只有 `FlushPolicy` 常量 | **无 chunk 合并、无背压窗口、无慢消费者降级** |
| retrieval-core | 13 行，只有 `FusionWeights` 常量 | **无混合检索融合、无 rerank、无证据去重、无 citation shaping** |
| fanout-core | 7 行，只有分区数推荐函数 | **无高基数 fanout、无通道分区、无广播调度** |
| patch-merge-core | 3 行，`supports_three_way_merge() -> true` | **无 diff/patch/merge/冲突打分** |
| memory-segment-core | 3 行，`compactable_window(n) -> n>=40` | **无消息段压缩、无窗口裁剪、无 summary checkpoint** |

> Rust 层没有任何 crate 被任何 Go 服务调用，也没有 FFI/gRPC/CLI 接入路径。

### 4. AI 与离线业务层（Python）— 完成度 ~5%

| 服务 | 现状 | 缺口 |
|------|------|------|
| offline-knowledge-service | `/healthz` + `/profile` 桩 | **无 ingestion、无 chunking、无 embedding、无 Qdrant upsert** |
| document-pipeline-service | 同上 | **无 PDF/DOCX/PPT/图片抽取、无预览产物、无内容清洗** |
| model-adapter-service | 同上 | **无 provider adapter、无批量 embedding/rerank** |
| evaluation-batch-service | 同上 | **无回归评测、无质量打分、无压测回放分析** |
| summarization-service | 同上 | **无离线 consolidation、无 session 总结、无报告生成** |

> 仅 [offline_knowledge_service](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/python/offline_knowledge_service/Dockerfile) 有 Dockerfile，其余 4 个无 Dockerfile，[docker-compose.platform.yml](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/deploy/docker-compose.platform.yml) 一个 Python 服务都没起。

### 5. 数据与检索层 — 完成度 ~15%

| 组件 | 现状 | 缺口 |
|------|------|------|
| Redis | session/tool-permission 用单机 client | 未上 Cluster；**无限流 token bucket key (`rl:*`) 未实现**；无 route 表 |
| NATS | [eventbus.go](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/shared/eventbus/eventbus.go) 用 `nats.Connect` + `Publish/Subscribe` | **未用 JetStream**（无 stream 持久化、无 durable consumer、无回放）；[data_plane.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/data_plane.json) 规划的 5 条 stream 全未创建 |
| PostgreSQL | compose 起了容器 | **没有任何服务连接 PG**；新 schema 零迁移；遗留单体仍用 SQLite/Neon |
| Qdrant | compose 起了容器 | **无任何 upsert/检索调用**；collection 未建 |
| OpenSearch | compose 起了容器 | **无任何索引/检索调用** |
| MinIO | compose 起了容器 | **无任何 bucket 操作** |

### 6. 可观测与运维层 — 完成度 0%

| 能力 | 状态 |
|------|------|
| OpenTelemetry trace | ❌ 无 instrumentation，事件带 `trace_id` 但无 span 上报 |
| Prometheus + Grafana | ❌ 无 metrics 暴露（Go 服务无 `/metrics`）、无 dashboard |
| Loki / ClickHouse logs | ❌ 仅 `log.Printf` 标准输出 |
| Tempo / Jaeger | ❌ 无 |
| ArgoCD + K8s + HPA/KEDA | ❌ `deploy/` 只有 1 个 docker-compose 文件，零 K8s manifest |
| 灰度/熔断/降级/回放 | ❌ 无 |

### 7. 遗留单体 — 仍在役

[app/](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/) 共 **120 个 Python 文件、约 30,362 行**，承载全部真实业务：
- `api/`：agent、chat、websocket、files、git、memory、tasks、canvas、im 等 18 个路由模块
- `services/`：message_router、task_state_machine、langgraph_workflow、orchestrator_preprocessor、agent_service、tool_executor、guardrails、result_synthesizer、websocket_manager 等 30+ 核心服务
- `services/adapters/`：cloudcode、local_claude、local_codex、local_openclaw 4 个模型适配器
- `services/memory/`：consolidator、extractor、scanner、session_memory、storage
- `services/tools/`：14 个工具模块（agent_tools、browser_tools、network_tools、permission、skill_tools 等）
- `services/protocols/`：claude_code、codex、openclaw 协议
- `db/`：SQLite/Neon 会话层

[README.md](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/README.md) 仍把 `python uvicorn main:app` 作为唯一启动方式，未提及新平台服务。

### 8. 测试与质量 — 完成度 0%

- `services/` 下**零 `*_test.go`、零 Rust 测试、零 Python 测试**
- 无 CI 配置（无 `.github/workflows`）
- 无契约测试、无压测脚本

---

## 三、未完成任务清单（按优先级排序）

### 🔴 P0 — 阻塞 Phase 1 收尾，必须先做

| # | 任务 | 涉及位置 | 验收标准 |
|---|------|----------|----------|
| P0-1 | gateway-service 补 WebSocket 网关 + JWT 鉴权 + 连接注册表 | [gateway-service/main.go](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/gateway-service/cmd/gateway-service/main.go) | 能用 `ws://` 建连、JWT 校验、连接写入 Redis `route:{tenant}:{session}` |
| P0-2 | eventbus 切到 JetStream，创建 5 条 durable stream | [shared/eventbus/eventbus.go](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/shared/eventbus/eventbus.go) | `nats.JetStreamContext` + `AddStream`，重启不丢消息 |
| P0-3 | session-service / tool-permission-service 接 PostgreSQL 持久化 | [session-service](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/session-service/cmd/session-service/main.go)、[tool-permission-service](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/tool-permission-service/cmd/tool-permission-service/main.go) | sessions/permissions/audit 三张表 + 迁移脚本，Redis 仍作热缓存 |
| P0-4 | stream-delivery-service 把 replay 从内存迁到 Redis Streams / JetStream | [stream-delivery-service/main.go:43](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/stream-delivery-service/cmd/stream-delivery-service/main.go#L43) | 重启后能按 cursor 回放最近 N 条 |
| P0-5 | Go 服务统一加 `/metrics`（Prometheus）+ OpenTelemetry trace 注入 | 全部 7 个 Go 服务 | trace_id 贯穿 gateway→orchestrator→runtime；Grafana 能看到 4 张基础图 |
| P0-6 | docker-compose 补 Python 5 个服务 + Rust stream-core + 可观测栈（OTel Collector、Prometheus、Grafana、Loki、Tempo） | [deploy/docker-compose.platform.yml](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/deploy/docker-compose.platform.yml) | `docker compose up` 一键起全栈 |

### 🟠 P1 — Phase 2 核心，让 Go 接管在线主链路

| # | 任务 | 说明 |
|---|------|------|
| P1-1 | realtime-orchestrator 实现真 ReAct 状态机 | 从 [app/services/task_state_machine.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/task_state_machine.py) + [langgraph_workflow.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/langgraph_workflow.py) 抽状态转换逻辑到 Go；budget/step cap 落地 |
| P1-2 | 多 Agent 角色拆分（Router/Planner/Executor/Critic/Summarizer/Search） | 现在只有一个 "router-agent" 占位；参考 [app/services/agent_service.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/agent_service.py) 与 [agent_route_service.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/agent_route_service.py) |
| P1-3 | agent-runtime-control-plane 接 Python model-adapter-service | 用 NATS request/reply 真正调 LLM，替换 [main.go:41](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/agent-runtime-control-plane/cmd/agent-runtime-control-plane/main.go#L41) 的回声结果；加 worker lease + 并发上限 |
| P1-4 | Python model-adapter-service 实现 provider adapter | 从 [app/services/adapters/](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/adapters/) 迁移 claude/codex/openclaw/cloudcode 适配器；统一 OpenAI 兼容协议 |
| P1-5 | gateway 接 SSE fallback + 慢客户端降级 | 弱网/浏览器限制场景 |
| P1-6 | 双轨迁移：流量镜像 + 前端可切换 | 前端 `/api/*` 支持环境变量切到 Go 网关；先镜像只读流量比对结果 |
| P1-7 | 四层限流（用户/租户/Agent/工具） | Redis token bucket + gateway 本地漏桶，落 `rl:{tenant}:{principal}:{bucket}` key |

### 🟡 P2 — Phase 3，Rust 核心与 DeepSearch

| # | 任务 | 说明 |
|---|------|------|
| P2-1 | Rust stream-core 实现 chunk 合并 + 背压窗口 + 慢消费者降级 | 当前 [lib.rs](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/rust/crates/stream-core/src/lib.rs) 10 行；用 tokio + channel；通过 gRPC 或 NATS 接入 stream-delivery |
| P2-2 | Rust retrieval-core 实现 BM25+dense+rerank 融合 | 调 OpenSearch（BM25）+ Qdrant（dense）+ Python rerank；输出 citation-ready 结果 |
| P2-3 | offline-knowledge-service 实现 ingestion→chunking→embedding→Qdrant upsert | 从 [app/services/memory/](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/memory/) + [text_processing.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/services/text_processing.py) 迁移 |
| P2-4 | document-pipeline-service 实现 PDF/DOCX/PPT/图片抽取 + 预览产物 | 从 [app/api/files.py](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/app/api/files.py) 迁移；产物入 MinIO |
| P2-5 | DeepSearch 端到端：query rewrite → multi-hop → hybrid → rerank → ground → synthesize | 对照 [platform/react_deepsearch_flow.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/react_deepsearch_flow.json) 与 [event-catalog.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/contracts/event-catalog.json) 的 retrieval 事件 |
| P2-6 | Rust fanout-core / patch-merge-core / memory-segment-core 填实现 | 当前各 3–7 行 |
| P2-7 | 检索/模型调用独立 worker pool，文件处理单独隔离 | 容量模型 [capacity_model.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/capacity_model.json) 已规定副本数 |
| P2-8 | 故障降级链：检索超时→轻检索、rerank 超时→原始融合、planner 超时→单 Agent、stream 过载→分段聚合 | |

### 🟢 P3 — Phase 4，商业化硬化

| # | 任务 | 说明 |
|---|------|------|
| P3-1 | 多租户隔离 + RBAC/ABAC + 敏感工具二次确认 UI | |
| P3-2 | 租户配额 + 账单 + LLM 预算控制 | |
| P3-3 | 审计全链路：PostgreSQL/ClickHouse 落库 + 回放 | audit-log-service 现仅内存缓冲 |
| P3-4 | Secret Manager / KMS + 数据脱敏加密 | |
| P3-5 | K8s manifest + Helm + ArgoCD + HPA/KEDA | 按 capacity_model 副本数 |
| P3-6 | 灰度发布 + 熔断 + 自动降级 + 故障演练 | |
| P3-7 | 万人在线压测 + 容量校准（10k 连接 / 2k 活跃会话 / 1k 并发 Agent / 首 token P95<2s / 推送 P95<80ms / 送达>99.99%） | 对照 [capacity_model.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/capacity_model.json) |
| P3-8 | summarization-service + evaluation-batch-service 实现 | 离线 consolidation、报告生成、回归评测 |

### ⚪ 横切项 — 全程并行

| # | 任务 | 说明 |
|---|------|------|
| X-1 | 每个服务加单元测试 + 契约测试 | 当前 `services/` 零测试 |
| X-2 | CI 流水线（lint + test + build + 安全扫描） | 当前无 `.github/workflows` |
| X-3 | 更新 [README.md](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/README.md) 反映双轨架构 | 现仍只写 `uvicorn main:app` |
| X-4 | 清理调试残留文件 | 根目录 `debug_avatar_loss.py`、`debug_dump_models.py`、`data/_diag_*.py`、`frontend/test-mermaid-*.{ts,txt}` 等 |
| X-5 | 统一 Dockerfile 模板 | 仅部分 Go 服务遵循 project_memory 的 apk+GOPROXY+tidy 规范；Python/Rust 缺 Dockerfile |

---

## 四、后续改进方案

### 4.1 总体策略：严格双轨，先北后南

遵循 [重构.md](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/重构.md) "风险与规避" 五条铁律，**禁止一次性重写**。建议按"先打通垂直切片，再水平铺开"的方式推进，避免长时间无法验证。

### 4.2 推进节奏（建议）

```
当前 → Sprint A (2 周)   Phase 1 收尾：P0-2/3/4/5/6 + P0-1 WebSocket
       ↓
       Sprint B (3 周)   垂直切片打通：1 条会话能从 Go 网关走完 ReAct→LLM→stream 回推
       ↓
       Sprint C (3 周)   Phase 2 横向铺开：P1-1/2/3/4/6/7，前端可灰度切流
       ↓
       Sprint D (4 周)   Phase 3：Rust stream-core + retrieval-core + DeepSearch 端到端
       ↓
       Sprint E (4 周)   Phase 4：多租户/审计/K8s/压测
```

### 4.3 关键技术决策

1. **Rust 接入方式**：优先用 **NATS 作为边界**（Go 发 NATS → Rust 订阅处理 → 回写 NATS），而非 FFI。这样语言边界清晰、可独立扩缩容、符合"Rust 只做性能热点不做编排"的铁律。Rust crate 内部用 tokio 异步。
2. **Python 退出在线主链路**：model-adapter-service 通过 NATS request/reply 供 Go 调用，**但 LLM 流式 token 仍由 Go stream-delivery 推给客户端**，Python 不持有客户端连接。这是"Python 不留在在线主链路"的关键。
3. **会话态外置**：ReAct 中间状态（当前阶段、已用 step、tool 历史）写 Redis `session:state:{tenant}:{session}` hash，PG 只存终态消息与审计。避免单机内存 session。
4. **JetStream 而非 core NATS**：[eventbus.go](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/shared/eventbus/eventbus.go) 现在用 core NATS，重启丢消息。所有 5 条 stream 必须建 JetStream durable，replay 才能工作。
5. **前端切流开关**：[next.config.js](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/frontend/next.config.js) 的 `rewrites` 目标用环境变量 `BACKEND_URL`，默认仍指 8000，灰度时切 8081。WebSocket 路径单独配置。

### 4.4 验收闸口（每 Sprint 必过）

| 闸口 | 指标 |
|------|------|
| Sprint A 结束 | `docker compose up` 起全栈 + 7 个 Go 服务 `/metrics` 可见 + JetStream 5 条 stream 持久化 |
| Sprint B 结束 | 1 条会话从 Go 网关 WS 进 → ReAct 走完 → LLM 真实回复 → stream-delivery 推回客户端；trace 在 Tempo 贯通 |
| Sprint C 结束 | 10% 流量灰度切到 Go 网关 24h 稳定，错误率 <0.1%，与遗留单体结果一致 |
| Sprint D 结束 | DeepSearch 端到端 P95 < 2s 首 token；Rust stream-core 背压在慢客户端下生效 |
| Sprint E 结束 | 万人压测达标（capacity_model 全部指标）；故障演练自动降级生效 |

### 4.5 风险与规避

| 风险 | 规避 |
|------|------|
| Rust 层长期空置成为瓶颈 | Sprint B 起就让 stream-core 接入最小路径（哪怕只做 chunk 透传），早暴露集成问题 |
| 遗留单体迁移过程中业务停摆 | 双轨：新链路先只读镜像，比对结果一致后再切写；保留 7 天回退窗口 |
| 多语言三套 envelope 漂移 | 把 [event-envelope.schema.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/contracts/event-envelope.schema.json) 作为契约源，加 CI 校验三端结构体字段一致 |
| JetStream 切换造成在途消息丢失 | 双写期：core NATS 与 JetStream 并行发，消费端切到 JetStream 后再停 core |
| 万人压测拿不到真实流量分布 | 用 [evaluation-batch-service](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/python/evaluation_batch_service/main.py) 回放历史会话日志生成压测脚本 |

### 4.6 立即可启动的 3 件事

1. **P0-2 JetStream 切换**（1–2 天，纯改 [eventbus.go](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/shared/eventbus/eventbus.go)），收益最大、风险最低，解锁所有持久化与回放。
2. **P0-6 compose 补全**（1 天），让全栈可一键起，后续每一步都能端到端验证。
3. **P0-5 可观测 bootstrap**（2–3 天），先加 `/metrics` + trace 注入，否则后续性能调优与降级判断无依据。

---

## 五、附录：评估证据索引

| 文件 | 用途 |
|------|------|
| [重构.md](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/重构.md) | 目标架构原始文档 |
| [platform/service_boundaries.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/service_boundaries.json) | 16 服务边界定义 |
| [platform/migration_roadmap.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/migration_roadmap.json) | 4 阶段路线 |
| [platform/data_plane.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/data_plane.json) | 数据平面规划 |
| [platform/capacity_model.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/capacity_model.json) | 容量与副本目标 |
| [platform/contracts/event-catalog.json](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/platform/contracts/event-catalog.json) | 事件契约 |
| [deploy/docker-compose.platform.yml](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/deploy/docker-compose.platform.yml) | 唯一部署文件 |
| [services/go/go.work](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/go/go.work) | Go 工作区（7 模块） |
| [services/rust/Cargo.toml](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/services/rust/Cargo.toml) | Rust workspace（6 crate） |
| [frontend/next.config.js](file:///d:/Users/xyn/Desktop/agenthub/AgenthubV1.2/frontend/next.config.js) | 前端 API 代理（仍指 8000） |
